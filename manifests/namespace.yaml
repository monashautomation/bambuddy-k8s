apiVersion: v1
kind: Namespace
metadata:
  name: bambuddy
  labels:
    # hostNetwork required for SSDP printer discovery -> needs privileged PSA level.
    pod-security.kubernetes.io/enforce: privileged
